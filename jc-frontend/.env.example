# Relative paths are production-safe when the UI is served behind the same origin.
VITE_API_BASE_URL=/api/v1
VITE_ADMIN_API_BASE_URL=/api/admin

# Optional Vite development proxy, for example http://localhost:8080.
VITE_DEV_BACKEND_URL=
