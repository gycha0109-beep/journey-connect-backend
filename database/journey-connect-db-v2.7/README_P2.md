# Journey Connect DB v2.7 — Recommendation P2

Canonical SQL `01..24` remains byte-identical to v2.6. SQL `25` adds deterministic experiment assignment/exposure, immutable evaluation datasets, versioned metrics, statistical results, Gate A–E, and append-only release/rollback decisions. SQL `26` validates bindings, privileges, append-only enforcement, and state transitions on PostgreSQL 15.
