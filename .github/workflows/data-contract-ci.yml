name: Data Contract CI

on:
  pull_request:
    paths:
      - "jc-data-contracts/**"
      - "jc-backend/settings.gradle.kts"
      - "docs/platform/data/DP-1-*.md"
      - "docs/platform/data/DP-2-*.md"
      - "verification/dp1/**"
      - "verification/dp2/**"
      - ".github/workflows/data-contract-ci.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: data-contract-ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  data-contracts:
    name: Java 21 / DP-1 and DP-2 contracts
    runs-on: ubuntu-latest
    timeout-minutes: 15

    defaults:
      run:
        working-directory: jc-backend

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Java 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v6

      - name: Grant execute permission for Gradle Wrapper
        run: chmod +x ./gradlew

      - name: Run Data contract gate
        shell: bash
        run: |
          set -o pipefail
          mkdir -p ../verification/dp2/runtime
          ./gradlew :jc-data-contracts:check --stacktrace --no-daemon 2>&1 \
            | tee ../verification/dp2/runtime/DP2_DATA_CONTRACT_CI.log

      - name: Run Recommendation Java Core regression
        shell: bash
        run: |
          set -o pipefail
          ./gradlew :jc-recommendation-core:check --stacktrace --no-daemon 2>&1 \
            | tee ../verification/dp2/runtime/DP2_RECOMMENDATION_CORE_CI.log

      - name: Upload Data contract evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dp2-data-contract-ci-evidence
          path: |
            verification/dp2/runtime/DP2_DATA_CONTRACT_CI.log
            verification/dp2/runtime/DP2_RECOMMENDATION_CORE_CI.log
            jc-data-contracts/build/reports/**
            jc-data-contracts/build/test-results/**
            jc-recommendation-core/build/reports/**
            jc-recommendation-core/build/test-results/**
          if-no-files-found: warn
