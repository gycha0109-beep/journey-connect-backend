# Evidence Retention

Committed Markdown and JSON evidence is retained in Git history. CI uploads exact-head verifier output and all OP-2 contract artifacts for 90 days.

Historical RCA-2 and OP-1 evidence is immutable. Incident evidence must bind tested SHA, workflow runs, artifact ID/digest and traffic/control state.
