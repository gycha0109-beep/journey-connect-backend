# Blocker Register

Active blockers: metrics backend, dashboard path, critical alert route, credential revoke provider, network control, external endpoint, external credential, external allowlist and candidate source/protocol/version.

Every blocker prevents OP-3 entry. No fake resource has been created.
