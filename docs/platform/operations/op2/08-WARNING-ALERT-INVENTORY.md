# Warning Alert Inventory

Warning alerts cover timeout, exception, queue rejection, late discard, task age, checkpoint lag, executor saturation, credential refresh failure, allowlist lookup failure and unexpected P1/P2 mismatch.

SC-6 thresholds are unchanged. Any change requires System Coordination reapproval.
