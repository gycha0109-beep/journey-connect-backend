# OP-1 Continuity Verification

Status: `PASS`.

OP-1 endpoint policy, TLS requirement, SSRF protection, workload credential abstraction, allowlist default deny, stable-hash ceiling, read-only candidate boundary and primary-authority rules are unchanged.

`verification/operations/op1/**` is protected from modification. Configured and effective traffic remain `0%`.
